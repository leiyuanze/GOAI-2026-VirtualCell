# -*- coding: utf-8 -*-
"""
GOAI 2026 赛道三 · 初赛方案文档生成器（v8 完整文章版·方法演进厚写）
官方模板五章；第四章按 v2.x → v3.x → v5.x 逐版本记录（动机/做法/结果/结论/失败实验），
含完整消融表、外部数据探索、评分口径说明与后续计划。
"""
from docx import Document
from docx.shared import Pt, RGBColor
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.enum.table import WD_TABLE_ALIGNMENT

doc = Document()
style = doc.styles['Normal']
style.font.name = 'Calibri'
style.font.size = Pt(11)

def para(text, size=11, bold=False, color=None, italic=False):
    p = doc.add_paragraph()
    r = p.add_run(text)
    r.font.size = Pt(size)
    r.font.bold = bold
    r.font.italic = italic
    if color:
        r.font.color.rgb = color
    return p

def bullet(text):
    doc.add_paragraph(text, style='List Bullet')

def table(headers, rows):
    tb = doc.add_table(rows=1 + len(rows), cols=len(headers))
    tb.style = 'Light Grid Accent 1'
    tb.alignment = WD_TABLE_ALIGNMENT.CENTER
    for j, h in enumerate(headers):
        c = tb.rows[0].cells[j]
        c.text = h
        for r in c.paragraphs[0].runs:
            r.font.bold = True
    for i, row in enumerate(rows):
        for j, v in enumerate(row):
            tb.rows[i + 1].cells[j].text = str(v)
    doc.add_paragraph()
    return tb

# ================= 封面 =================
t = doc.add_heading('虚拟酵母扰动响应预测：从残差分解到实体外推', level=0)
t.alignment = WD_ALIGN_PARAGRAPH.CENTER
sub = doc.add_paragraph()
sub.alignment = WD_ALIGN_PARAGRAPH.CENTER
r = sub.add_run('GOAI 2026 · 赛道三（前沿探索 AI for Research）· 算法赛题 · 虚拟细胞方向\n'
                '初赛方案说明文档与技术路线概述')
r.font.size = Pt(13)
r.font.color.rgb = RGBColor(0x44, 0x44, 0x44)
doc.add_paragraph()

# ================= 一、项目概述 =================
doc.add_heading('一、项目概述', level=1)
doc.add_heading('1.1 项目名称', level=2)
para('虚拟酵母扰动响应预测：条件响应分解与可迁移实体特征的蛋白质组建模')
doc.add_heading('1.2 参赛方向', level=2)
para('算法赛题 · 方向一：虚拟细胞（虚拟酵母扰动响应预测）')
doc.add_heading('1.3 方案概述', level=2)
para(
    '本方案面向酿酒酵母扰动蛋白质组预测任务：给定菌株、化合物、培养基、温度、时间与测量上下文，'
    '预测完整 log2 蛋白质组响应向量。这是一个典型的小样本（训练约 5900 样本）、高维（4422 个'
    '可评蛋白）、强外推（测试含未见化合物/菌株/双重未知组合）的回归问题。'
)
para(
    '我们把任务的本质理解为"状态恢复 + 扰动外推"两个子问题，并据此确立方法骨架：'
    '预测被显式拆分为状态模型（匹配对照丰度）与扰动模型（Δ），扰动部分用低秩子空间表达；'
    '对未见实体不依赖 ID 记忆，而依赖可迁移的结构（Morgan 指纹、RDKit 描述符）、序列（ESM2）、'
    '功能（GO 通路）与遗传（SNP 距离）特征。'
)
para(
    '方案历经三个阶段的演进，每一阶段都有明确的动机、验证与结论（含大量负面结论）：'
    'v2.x（8/5~8/7）建立"残差分解 + 对照监督 + 组件监督"骨架，经历 10 个版本与完整消融；'
    'v3.x（8/12~8/15）在统一开放榜规则下系统性探索外部结构化先验，GO 通路注意力成为最强单模型'
    '组件，并完成 8 个版本的探索与归因；v5.x（8/16）完成全链路数据泄漏审计与修复、验证 '
    'control/delta 分离架构的边界、落地组件级监督、RDKit 结构描述符与菌株遗传距离特征，'
    '并按实施方案逐字落实剩余步骤（对照回退、交互编码、PCA 联合表示、θ 门控、方向损失、'
    'top-k 网格等）。完整演进过程见第四章，后续计划见 3.3 节。'
)

# ================= 二、科学问题理解 =================
doc.add_heading('二、科学问题理解', level=1)
doc.add_heading('2.1 科学问题与研究对象', level=2)
para(
    '核心科学问题：在有限的扰动蛋白质组数据下，能否构建一个泛化到未知化学扰动与未知遗传背景'
    '的虚拟酵母模型——准确预测完整蛋白质组响应，并识别关键差异表达蛋白、药物响应模式与'
    '潜在调控机制。这一能力是"AI 预测 → 湿实验验证"闭环（菌株定向改造、药物响应预筛）'
    '的关键前置。'
)
para(
    '官方评测把泛化能力拆为三类核心场景：S1 已见菌株 + 未见化合物（新扰动外推）、'
    'S2 未见菌株 + 已见化合物（新遗传背景外推）、S3 双重未知组合，另含时间外推；'
    '六模块加权中约 65% 的权重围绕"处理 − 对照"的扰动响应 Δ（匹配对照原始 FC 25%、'
    '上下文均值残差 20%、药物均值残差 20%）。这意味着模型的真正考验不是拟合绝对丰度，'
    '而是刻画"这个化合物在这个菌株上引发了什么独特变化"。'
)
para(
    '从数据出发，任务有三个本质难点：'
    '（1）小样本高维——数千样本对数千蛋白，直接回归极易过拟合到"恢复群体平均状态"而丢失'
    '个体扰动信号，因此必须对任务做结构分解；'
    '（2）实体外推——未见菌株/化合物没有可学的 ID 嵌入，只能依赖可迁移特征（结构、序列、'
    '遗传距离）或上下文统计先验，而评测最难的正是未见菌株场景；'
    '（3）测量上下文混杂——数据来源、仪器、板号等测量变量与生物条件混叠，模型需吸收测量'
    '变异而不被其误导。'
)
doc.add_heading('2.2 科学意义', level=2)
para(
    '模型的科学意义体现在三处：'
    '一是为未见化合物/菌株的蛋白质组响应提供可外推的定量预测（预测 Δ 与真实差异表达'
    '显著相关，且富集到已知生物学通路，FDR<0.05）；'
    '二是把"遗传近的菌株响应可迁移"这一生物学直觉显式编码进模型（测试集新菌株 CRD 与'
    '训练菌株 CGD 的 SNP 距离仅 0.383，遗传近邻成为外推的关键信号）；'
    '三是通过严格的泄漏审计与消融，沉淀该数据尺度下的方法边界——哪些组件有效、哪些无效、'
    '为什么，对社区构建同类小样本高维外推任务具有参考价值。'
)

# ================= 三、技术方案与预期方法路线 =================
doc.add_heading('三、技术方案与预期方法路线', level=1)

doc.add_heading('3.1 总体技术方案', level=2)
para(
    '总体形式：ŷ(s,c,k) = ŷ_ctrl(s,k) + Δ̂(s,c,k)，其中 ŷ_ctrl 为匹配对照预测（状态模型），'
    'Δ̂ 为扰动响应（扰动模型），k 为培养基/温度/时间/平台上下文。扰动部分采用低秩表达：'
    'Δ̂ = U z + r，U 为训练集 Δ 的 SVD 响应基（方差解释约 48%），z 为低维扰动状态，'
    'r 为带强正则的蛋白级残差。'
)
bullet(
    '状态模型（control）：仅用非化合物信息（菌株、培养基、温度、时间、平台/板/来源、上下文'
    '先验）预测匹配对照丰度。时间编码采用 log2 + sin/cos 周期项。三阶段训练：先预训练状态模型，'
    '再固定它训练扰动模型，最后联合微调（分组学习率，control 1e-4 / response 3e-4）。'
)
bullet(
    '扰动模型（response）：菌株、化合物（Morgan 指纹 + RDKit 描述符或拼接 PCA 表示）、'
    '上下文编码 → 低维状态 z → 低秩输出 Δ̂ = U z + r，叠加 GO 通路效应与 ESM2 序列先验；'
    'v5.7 起加入菌株×化合物/化合物×上下文/菌株×上下文的交互编码。'
)
bullet(
    '上下文先验 ctx_prior：菌株×培养基×温度×时间分组均值作为蛋白空间先验（train-only），'
    '为未见菌株提供"细胞状态锚点"，是经验贝叶斯式的保守外推。'
)
bullet(
    '菌株遗传特征：由 1011 酵母菌株 SNP 距离矩阵构建"到训练菌株的遗传距离向量"，'
    '使未见菌株可借遗传近邻迁移响应（CRD–CGD 距离 0.383）。'
)
bullet(
    '结构化先验：GO 通路注意力（92 个高频通路，同一通路蛋白共享响应偏置）与 ESM2 蛋白序列'
    '先验，作为功能层面的软约束。'
)
para(
    '损失函数采用分层组合：绝对丰度 MSE、Δ 监督 MSE、FC 相关损失（对齐 Δ 预测与 Δ 真值）、'
    '蛋白谱相关损失、GO 通路图正则、留一组件残差监督（Δ 对齐去掉自身后的 μ_ctx/μ_drug 残差）、'
    '残差头 L2 正则，以及可选的 L_rank 方向损失（BCE，v5.6 验证持平）。'
    '对照匹配采用 5 级回退（精确 → 同菌株/培养基/温度/时间 → 同菌株/培养基/温度 → '
    '同菌株/培养基 → 全局），并将 control_quality 作为 Δ 监督的样本权重（v5.7，覆盖从 79% '
    '提升到 100%）。'
)
para(
    '门控设计经历了多次迭代：v2.x 的 seen/unseen 硬门控 → v5.3 相似度/支持数驱动的连续门控'
    '（负面）→ v5.6 θ 参数化可靠性门控（持平）。三轮实验一致表明：在当前数据尺度下门控形式'
    '并非性能瓶颈，这一负面结论本身是有价值的边界界定。'
)

doc.add_heading('3.2 关键设计动机', level=2)
para(
    '三个设计决策源于对评测与数据的理解：'
    '（1）拆分为状态 + 扰动，是因为 65% 的评分权重围绕 Δ，直接回归绝对丰度会稀释扰动信号；'
    '（2）低秩表达，是因为 4422 维输出头在小样本下极易过拟合，而 Δ 的共变结构（SVD 方差'
    '48%）足以承载主要扰动模式，残差头用强正则控制；'
    '（3）可迁移特征优先于 ID 记忆，是因为未见实体无法学习 ID 嵌入，而结构/序列/遗传特征'
    '在实体间共享，是外推的唯一信息源。'
)

doc.add_heading('3.3 预期方法路线（后续计划）', level=2)
para('初赛阶段以方案设计与初步验证为主；以下为后续计划按优先级排列的路线。', bold=True)
table(
    ['优先级', '方向', '具体内容', '预期收益'],
    [
        ['P1', 'leave-entity-out episodic training',
         '在训练中随机留出化合物/菌株实体，把其样本按"未见"处理并加权扰动损失，使训练目标'
         '直接对应 S1/S2/S3。初赛已实现并启动初版实验（未见提升，原因可能是同批双状态折中与'
         'β 设置），复赛将按"episode 内实体整体留出 + β 网格"重做',
         '直接优化外推目标，可能提升 S2/S3'],
        ['P1', '自适应化学迁移校准',
         '把融合系数 α 从"相似度分段"升级为可学习校准器（输入：相似度、支持数、上下文匹配、'
         '不确定性），并加入"响应谱相似性迁移"分支；top-k 网格已表明 k=5~10、α 高值更优',
         '新化合物场景更稳健'],
        ['P1', '菌株功能聚合特征',
         '将拷贝数/功能注释映射到通路层面（pathway burden、TF target score），配合现有 SNP '
         '距离核；受"仅有距离矩阵、无原始位点"数据限制，将优先利用官方与公开的菌株注释资源',
         '进一步增强 S2 外推'],
        ['P2', '蛋白模块化输出',
         '将 4422 维输出改为"通路/模块状态 → 蛋白残差"两层结构（GO 模块或数据驱动模块），'
         '替代当前低秩 + 独立残差头的组合',
         '提升蛋白谱一致性与可解释性'],
        ['P2', '不确定性估计',
         '引入深度集成 / MC-dropout / 距离感知不确定性，输出 ŷ ± σ，支持虚拟筛选中的'
         '"哪些预测可信"判断',
         '科学价值与报告严谨性'],
        ['P2', '时间动力学建模',
         '对时间外推场景引入显式动力学项（如时间序列模块或连续时间编码增强），'
         '当前时间外推已较好（val_time R² 0.83），作为后续增强',
         '时间场景进一步稳健'],
    ]
)

doc.add_heading('3.4 数据来源、依赖工具与运行流程', level=2)
para(
    '数据来源：官方 train_val 与 test（2026-07-15 发布版），仅 train 划分用于训练与全部统计量'
    '估计；外部公开资源包括 UniProt GO 注释、ESM2 蛋白序列嵌入、STRING 蛋白互作、'
    '1011 酵母菌株 SNP 距离矩阵、PubChem 化合物 SMILES。'
)
para(
    '依赖工具：Python 3.9 / PyTorch（CUDA）/ scikit-learn（SVD、PCA）/ RDKit（指纹与描述符）/ '
    'pandas / numpy。运行流程：数据预处理 → 特征构建 → 三阶段训练（v2.1/v3.5/v3.7/v5.x）→ '
    '四场景评估（5 个随机种子）→ 提交文件生成。'
)

# ================= 四、阶段性实验结果 =================
doc.add_heading('四、阶段性实验结果或可行性验证', level=1)
para(
    '本节按时间顺序完整记录方法演进的动机、做法、结果与结论，包括大量负面实验——它们同样'
    '构成方法边界的重要信息。所有统计量（缺失率、均值、标准化、SVD、迁移池、对照匹配）'
    '均仅从 train 划分计算；验证集为官方划分的四类外推场景。',
    italic=True,
)

# ---------- 4.1 v2.x ----------
doc.add_heading('4.1 v2.x：残差分解骨架的建立（10 个版本消融）', level=2)
para(
    '动机：评测大量基于 Δ（65% 权重），直接预测绝对丰度会掩盖扰动信号；同时 4422 维输出'
    '在小样本下极易过拟合。因此第一阶段的思路是：把预测任务按"基线/共享应激/化合物特异/'
    '菌株调制"做残差分解，让架构携带对评测目标的归纳偏置。'
)
para('版本演进与消融结果（val 蛋白 R² 中位数，chem/strain/both/time）：')
table(
    ['版本', '关键改动', '结果', '结论'],
    [
        ['v2.0', '基础残差分解 B/S/C/T + MSE + FC corr', 'val_both 0.65', '起点'],
        ['v2.1', '+对照监督(751 对照) + 组件级监督(LOO μ_ctx/μ_drug)', '0.874/0.640/0.758/0.824', '基准最优'],
        ['v2.2', '+解耦一致性正则 + 实体 Dropout', '无提升略降', '解耦靠 loss 硬拉无效'],
        ['v2.3', 'gate 0.05 + latent 128 + 去 hash', '崩溃', '数据索引 bug（make_x 参数错位），修复后仍无提升'],
        ['v2.4', '教程 loss 全项（逐蛋白 corr/高效应 corr 等）', '无提升', '蛋白相关 loss 实现恒 0'],
        ['v2.5', '输入级分离（C 只吃化合物侧、T 只吃菌株侧）', '0.876/0.607/0.676/0.833', '新菌株场景变弱，双刃剑'],
        ['v2.6', 'T 分支加 ctx_prior + unseen 门控归零', 'val_strain 0.645', 'B+S 分支对 unseen 菌株 embedding 错误'],
        ['v2.7', '全 ctx_prior + unseen 菌株均值 embedding 兜底', '0.877/0.650/0.737/-', '比 clamp 到菌株 0 更稳'],
        ['v2.8', '退火菌株/化合物 Dropout（模拟 unseen）', 'val_strain 0.650→0.506', '无基因组替代时强行"忘记"菌株只会学歪'],
        ['v2.9', '菌株×化合物交互项 + Kendall 自适应 loss', 'val_strain 0.671(+0.021)', '交互项有效'],
    ]
)
para(
    '最优组合：3×v2.1（seed 42/43/44）集成，val 四场景约 0.879/0.656/0.744/0.831。'
    '本阶段沉淀的关键教训：'
    '（1）后处理对照校准实测有害（重复校准破坏绝对水平，蛋白 R² 变负）——对照监督必须内置于模型；'
    '（2）共表达谱聚类因相关矩阵未 mask-aware（99.6% 假 0）而失效，模块分组是噪声；'
    '（3）训练 sanity check 重要——v2.3 的 loss 卡在 0.74 正是数据错位信号；'
    '（4）v2.8 从反面验证了外部表征对菌株外推的必要性，直接引出了 v3.x 的外部数据探索。'
)

# ---------- 4.2 v3.x ----------
doc.add_heading('4.2 v3.x：外部结构化先验（统一开放榜）', level=2)
para(
    '背景：8/11 官方规则修订，封闭榜/开放榜合并为统一开放榜，外部资源（菌株基因组、化合物'
    '结构、通路注释）全部允许使用（须披露来源版本）。这为"可迁移实体特征"提供了数据基础。'
)
para('外部数据系统性探索（区分"方法失败"与"数据无效"）：')
table(
    ['外部数据', '用法', '结论（含归因）'],
    [
        ['Morgan 指纹（PubChem+RDKit）', '2048 位 Tanimoto 迁移',
         '初版裸迁移≈0 被误判"无效"；实为方法问题（未条件对齐/单近邻）。改用 PCA-64 + 同菌株'
         '条件对齐 + top-k 加权后，test 迁移 PCC 平均 0.127，融合 +0.007'],
        ['ESM2 蛋白序列（320 维）', '输出层低秩接入',
         'gate_esm≈0.28（保留约 1/4），说明该接入方式增益有限，不能外推为序列信息无效'],
        ['菌株 SNP 距离（1011 项目）', '遗传相似度迁移',
         '证据有限（手工近似距离矩阵、混淆背景基线 vs 扰动调制）；后证实遗传距离核在 v5.2 有效'],
        ['菌株拷贝数（1011 项目）', '拷贝数相似度迁移', '5 菌株拷贝数几乎相同（相似度>0.91），无区分度'],
        ['大模型 + 完整 ESM2', 'hidden384/latent128', '小样本过拟合，val_both 0.748 退步'],
        ['PPI 图正则（STRING）', '约束绝对丰度', '初版实现错误（0.05 权重 20000 边）崩溃；Δ 协同版仍无效'],
    ]
)
para('有效方向：蛋白侧结构化先验（GO/KEGG 通路）')
bullet(
    'v3.5 蛋白先验 loss（逐蛋白 corr + 协方差低秩正则）：val_strain +0.003、val_time +0.005，'
    '方向正确但增益小。'
)
bullet(
    'v3.7 GO 通路注意力（核心提升）：下载 UniProt GO 生物过程注释，构建 92 个高频 GO term→'
    '蛋白矩阵（覆盖 59% 蛋白），模型输出叠加"通路效应 @ 通路矩阵"，同一通路蛋白共享响应偏置。'
    'val_strain 0.662→0.675（+0.013）、val_both 0.752→0.762（+0.010）——正好打在未见菌株'
    '这一核心难点上，成为最强单模型。'
)
para('其余版本探索与归因（对比 v37 基准 0.869/0.676/0.760/0.829）：')
table(
    ['版本/方向', '结果', '根因'],
    [
        ['v3.8 大模型（hidden384/latent128）', '0.862/0.674/0.748/0.822', '小样本过拟合'],
        ['v3.9 PPI 图正则（0.05 权重 20000 边）', '0.793/0.514/0.629/0.735', '约束绝对丰度（基线差异主导）+ 权重过大'],
        ['v3.10 GO 阈值 258', '0.868/0.653/0.751/0.824', 'GO 92 已验证最优，258 过宽退步'],
        ['v4.1 GO 仅 unseen 门控', '0.868/0.598/0.714/0.823', 'GO 对 seen 菌株也有帮助'],
        ['v4.2 PPI Δ 协同正则', '无效', '强制相等是错误先验（协同≠相等）'],
        ['v4.3/v4.7 排序 loss', '无效', '与 fc_corr 冗余；M6 DEP 方向当前数据无效'],
        ['v4.4 残差形状监督（LOO μ）', '大降', 'LOO μ 噪声大 + 权重失衡'],
        ['v4.5 GO 门控重激活微调', '无效', 'gate_go 0.21 是模型主动选择的最优'],
        ['v4.6 评测权重再平衡（MSE 0.3/FC 0.35）', '大降', '绝对水平是 Δ 指标根基，MSE 不可降'],
    ]
)
para(
    '阶段洞察：单模型与集成各有优势且不可兼得——v3.7 单模型 val_strain 强（0.676）、'
    'val_chem 弱（0.869）；集成 val_chem 强（0.879）、val_strain 弱（0.667）。'
    '评测按 split_final 分四场景，可采用"场景自适应"映射让每个场景用最强组合（该策略属'
    '竞赛选择而非可复现科学方法本身，本方案如实区分并保证全链路 train-only）。'
)

# ---------- 4.3 v5.x ----------
doc.add_heading('4.3 v5.x：泄漏审计与重构（8/16）', level=2)
para(
    '动机：在冲刺前按评审意见完成 P0 泄漏审计，并验证"状态模型 + 低秩扰动模型"这一结构化'
    '重构路线；随后按实施方案逐字落实剩余步骤。'
)
bullet(
    'P0 泄漏审计：发现并修复 3 处统计量越界——chem_emb_init 曾用 val 真值（6 个 val-only '
    '化合物的初始特征直接编码了其真实平均响应）、matched control 曾跨划分（12 个训练样本'
    '对照来自 val）、迁移池曾含 val 对照。全部改为 train-only，确保后续结论可信。'
)
bullet(
    'v5.0 control/delta 低秩分离：状态模型 + 扰动模型（U@z_delta）。实证：分离单模型'
    '（0.810/0.491/0.581/0.776）弱于 v37 共享编码直接回归（0.864/0.672/0.754/0.826）——'
    '该数据上"共享编码 + 直接回归"更强；但 v5.0 作为集成成分有互补价值（0.8v37+0.2v50 '
    '集成 unseen 菌株 +0.010）。'
)
bullet(
    'v5.1 组件级监督 + RDKit 描述符：补留一 μ_ctx/μ_drug 残差监督 + 10 项结构描述符，'
    '单模型 strain +0.089、both +0.094（0.822/0.580/0.675/0.777）——组件监督对未见菌株的'
    '残差学习显著有效。'
)
bullet(
    'v5.2 菌株遗传特征：1011 酵母 SNP 距离矩阵 → 菌株到训练菌株的遗传距离向量。'
    '关键生物学信号：测试新菌株 CRD 与训练菌株 CGD 距离仅 0.383。单模型 strain +0.038、'
    'both +0.031（0.833/0.618/0.706/0.782）；0.75×v3.7+0.25×v5.2 集成四场景'
    '0.870/0.690/0.761/0.833 为统一评估对象。'
)
bullet(
    'v5.3 可靠性门控（相似度/支持数驱动）→ 负面（0.832/0.612/0.704/0.785），与 v4.9 一致，'
    '门控非瓶颈。'
)
bullet(
    'v5.4 训练配置修正（weight_decay 1e-4、阶段 C 分组学习率、残差头 L2 正则）→ 持平'
    '（0.834/0.613/0.704/0.780），配置参数不损害性能。'
)
bullet(
    'v5.6 θ 参数化门控 + L_rank 方向损失：g_c=σ(θ0+θ1·seen+θ2·sim+θ3·log1p(n)) 可学习'
    '门控 + BCE 方向损失 → 持平（0.832/0.617/0.705/0.781），门控与排序非瓶颈再确认。'
)
bullet(
    'v5.7 对照回退 + quality 加权 + 交互编码：对照匹配 5 级回退（覆盖 79%→100%，质量均值'
    '0.836）+ control_quality 样本加权 + 菌株×化合物/上下文交互编码 → 持平'
    '（0.832/0.616/0.705/0.769），数据利用率与架构完整性提升。'
)
bullet(
    'v5.8 Morgan+desc 拼接 PCA-32：2048 指纹 + 10 描述符 → PCA(32, whiten)（方差 96.3%）'
    '替换独立 morgan64 + desc 投影 → 持平（0.833/0.614/0.704/0.773）。'
)
bullet(
    'episodic training 初探：leave-entity-out 训练目标直接对应 S1/S2/S3 → 初版未见提升'
    '（阶段 B 中期 0.783/0.562/0.672/0.758，低于 v5.2 同期），可能源于同批双状态折中与'
    'β 设置，列为 P1 后续方向而非已定论。'
)

# ---------- 4.4 当前结果 ----------
doc.add_heading('4.4 当前结果', level=2)
para('（1）统一方法评估：0.75×v3.7 + 0.25×v5.2 集成，5 个随机种子（1/2/42/43/44）均值±标准差：')
table(
    ['场景', 'v5.2 单模型 R²', 'v5.2 FC PCC', '集成 R²', '集成 FC PCC'],
    [
        ['val_chem_only（S1 新化合物）', '0.829±0.004', '0.338±0.007', '0.869±0.000', '0.426±0.001'],
        ['val_strain_only（S2 新菌株）', '0.581±0.040', '0.280±0.014', '0.690±0.002', '0.355±0.004'],
        ['val_both（S3 双重未知）', '0.669±0.034', '0.223±0.010', '0.762±0.001', '0.234±0.003'],
        ['val_time（时间外推）', '0.779±0.002', '0.512±0.005', '0.832±0.000', '0.611±0.001'],
    ]
)
para(
    '集成结果跨 5 个种子标准差 ≤0.004，成绩稳定；v5.2 单模型在未见菌株场景存在 0.03 量级'
    '种子波动，属于小样本实体的固有噪声，如实报告。'
)
para(
    '（2）测试集真值自评（官方允许"可自评不可训练"，全链路 train-only）。按官方六模块'
    '（绝对保真 20% / 匹配对照 FC 25% / 上下文均值残差 20% / 药物均值残差 20% / '
    '双重未知与时间 10% / 高效应蛋白 DEP 检出 5%）：'
)
table(
    ['场景', 'M1 FC', 'M2 绝对', 'M3 ctx', 'M4 drug', 'M5 双盲/时间', 'M6 DEP', '总分'],
    [
        ['test_chem_only', '0.468', '0.922', '0.397', '0.468', '—', '0.624', '0.506'],
        ['test_strain_only', '0.827', '0.769', '0.827', '0.826', '—', '0.682', '0.725'],
        ['test_both', '0.556', '0.808', '0.556', '0.556', '0.605', '0.636', '0.615'],
        ['test_time', '0.624', '0.917', '0.561', '0.562', '0.779', '0.702', '0.677'],
    ]
)
para(
    '加权总分 ≈ 0.626（四场景权重 1/1/0.5/0.5，近似官方组合）。'
    '关于评分口径的说明：早期版本报告的 0.6349 使用旧 M6（DEP）口径——方向准确率与高效应 PCC '
    '各占 50%，未含召回/F1 惩罚；按官方规则"Recall 不单用，须与 precision、F1 或 AUPRC 组合，'
    '避免全报阳性刷高召回"，我们把 M6 修正为"方向准确率 40% + 高效应 PCC 30% + '
    'F1@K（50/100/200 三档均值）30%"，修正后加权总分约 0.626。'
    '分数差异完全来自评分口径的收紧（DEP 模块约 -0.009），而非模型性能变化——'
    '我们选择报告更严谨、符合官方要求的口径。'
)
para(
    '（3）科学层面验证：预测 Δ 显著富集到 4 个已知生物学通路（GO 富集，FDR<0.05），'
    '说明模型捕捉到功能层面的响应模式而非纯数值拟合；菌株遗传距离与响应相似度的正相关'
    '支持"遗传近 → 响应可迁移"。'
)
para(
    '（4）化学迁移 top-k 网格（val 伪新化合物验证）：k=5~10 最优（k=3 偏小），'
    'α 高值区间（0.3~0.5）更优，为复赛的迁移校准器设计提供依据。'
)

# ---------- 4.5 负面结论 ----------
doc.add_heading('4.5 负面结论汇总（方法边界）', level=2)
para(
    '门控形式（硬/连续/θ 参数化）非瓶颈（v4.9/v5.3/v5.6 一致）；排序方向损失无效'
    '（v4.3/v4.7/v5.6）；大模型过拟合（v3.8）；Dropout 降低效果；后处理对照校准有害；'
    '共表达谱聚类因 mask 错误失效；原始 SNP/CNV 直接接入无效（功能聚合才有价值）；'
    'PPI 图正则（绝对/Δ 协同两种形式）无效；exp(τ) 迁移权重不如 max 加权；'
    '低秩维度 d=32 与 d=64 持平；episodic 初版未见提升。'
    '这些结论共同界定了该数据尺度下的方法边界，是后续计划的重要输入。'
)

# ================= 五、复现与开放计划 =================
doc.add_heading('五、复现与开放计划', level=1)
doc.add_heading('5.1 复现方式', level=2)
para(
    '代码仓库（GitHub 公开，初赛按规则不强制提交代码，先提供开源计划）按数据/特征/训练/'
    '评估/提交组织；训练与评估固定随机种子（v2.1: 42/43/44；v3.7: 42；v5.2: 1/2/42/43/44），'
    '全部统计量仅从 train 划分计算；README 提供从数据预处理到提交文件生成的完整命令序列。'
    '复现环境：Python 3.9 + PyTorch + RDKit + scikit-learn，单卡 GPU。'
)
doc.add_heading('5.2 开源计划', level=2)
para(
    '复赛阶段开放完整可运行仓库：训练/评估/提交脚本与模型权重全部开源；'
    '外部资源（GO/ESM2/STRING/SNP 距离/PubChem SMILES）披露来源、版本与获取方式；'
    '官方数据遵循数据使用协议，不随仓库分发。'
)
doc.add_heading('5.3 依赖、数据来源与合规披露', level=2)
bullet('官方数据：train_val + test（蛋白维度 5243），仅 train 划分用于训练与统计量估计；测试真值仅用于最终自评（官方允许"可自评不可训练"）。')
bullet('外部资源：UniProt GO 注释、ESM2 蛋白嵌入、STRING 蛋白互作（v11.5）、1011 酵母菌株 SNP 距离矩阵（Bergström et al. 2014）、PubChem 化合物 SMILES（11 个未见化合物）。')
bullet('依赖：PyTorch、scikit-learn、RDKit（BSD 3-Clause）、pandas/numpy（BSD）；无商业 API、无闭源模型。')
bullet('合规自检：对照匹配、分组均值、SVD、迁移池全部 train-only；未见化合物指纹由 SMILES 生成（不依赖测试真值）；复现由固定种子 + 完整命令序列保证。')

doc.save(r'D:\leiyuanze\Datawhale\AI for Research\虚拟细胞\初赛方案文档.docx')
print('Saved: 初赛方案文档.docx')
