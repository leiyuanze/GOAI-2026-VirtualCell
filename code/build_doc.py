# -*- coding: utf-8 -*-
"""
GOAI 2026 赛道三 · 初赛方案文档生成器（v5 叙事版）
按官方模板五章结构：一、项目概述；二、科学问题理解；三、技术方案与预期方法路线；
四、阶段性实验结果或可行性验证；五、复现与开放计划。
叙事主线：问题 → 数据与评测 → 方法演进（每步动机/验证/失败）→ 统一方法 vs 竞赛策略 → 初步结果 → 预期路线 → 复现合规。
"""
from docx import Document
from docx.shared import Pt, RGBColor
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.enum.table import WD_TABLE_ALIGNMENT

doc = Document()
style = doc.styles['Normal']
style.font.name = 'Calibri'
style.font.size = Pt(11)

def para(text, size=11, bold=False, color=None):
    p = doc.add_paragraph()
    r = p.add_run(text)
    r.font.size = Pt(size)
    r.font.bold = bold
    if color:
        r.font.color.rgb = color
    return p

def bullet(text):
    doc.add_paragraph(text, style='List Bullet')

def table(headers, rows):
    tb = doc.add_table(rows=1 + len(rows), cols=len(headers))
    tb.style = 'Light Grid Accent 1'
    tb.alignment = WD_TABLE_ALIGNMENT.CENTER
    for j, h in enumerate(headers):
        c = tb.rows[0].cells[j]
        c.text = h
        for r in c.paragraphs[0].runs:
            r.font.bold = True
    for i, row in enumerate(rows):
        for j, v in enumerate(row):
            tb.rows[i + 1].cells[j].text = str(v)
    doc.add_paragraph()
    return tb

# ================= 封面 =================
t = doc.add_heading('虚拟酵母扰动响应预测：从残差分解到实体外推', level=0)
t.alignment = WD_ALIGN_PARAGRAPH.CENTER
sub = doc.add_paragraph()
sub.alignment = WD_ALIGN_PARAGRAPH.CENTER
r = sub.add_run('GOAI 2026 · 赛道三（前沿探索 AI for Research）· 算法赛题 · 虚拟细胞方向\n'
                '初赛方案说明文档与技术路线概述')
r.font.size = Pt(13)
r.font.color.rgb = RGBColor(0x44, 0x44, 0x44)
doc.add_paragraph()

# ================= 一、项目概述 =================
doc.add_heading('一、项目概述', level=1)

doc.add_heading('1.1 项目名称', level=2)
para('虚拟酵母扰动响应预测：基于条件响应分解与实体外推的蛋白质组预测模型')

doc.add_heading('1.2 参赛方向', level=2)
para('算法赛题 · 方向一：虚拟细胞（虚拟酵母扰动响应预测）')

doc.add_heading('1.3 方案概述', level=2)
para(
    '本方案针对"给定菌株、化合物、培养基、温度、时间与测量上下文，预测完整 log2 蛋白质组响应"'
    '这一任务。核心科学问题是：在有限的扰动蛋白质组数据（训练约 5900 样本、4422 个可评蛋白）下，'
    '如何构建能稳健外推到"未见化合物（S1）/未见菌株（S2）/双重未知（S3）"的模型。'
)
para(
    '我们采用"残差分解 + 上下文先验 + 结构化先验"的建模骨架，历经三个阶段的演进：'
    '（1）v2.x：绝对丰度按基线/共享应激/化合物特异/菌株调制四分支残差分解，配合对照监督与'
    '留一组件监督；（2）v3.x：引入 Morgan 指纹、ESM2 蛋白序列先验、STRING 蛋白互作图与'
    'GO 通路注意力，其中 GO 通路注意力对未见菌株场景的提升最大；（3）v5.x：先完成全链路'
    '数据泄漏审计与修复（3 处统计量越界全部收敛到 train-only），再按"状态模型 + 低秩扰动模型"'
    '重构（v5.0~v5.2），并加入 RDKit 结构描述符与菌株 SNP 遗传距离特征，最后以 5 个随机种子'
    '报告均值±标准差。'
)
para(
    '需要说明的是：我们区分"统一方法"与"竞赛提交策略"两个层面。统一方法层面，'
    'v3.7（GO 通路注意力直接回归）与 v5.2（control/delta 分离）代表两种建模路线，'
    '前者为最终得分贡献主体；竞赛提交层面，为在四类泛化场景各自取得更稳健的成绩，'
    '按场景选择不同的模型组合并叠加化学结构相似度迁移融合。这一策略是为了竞赛评分的'
    '务实选择，其代价是"按场景挑模型"不属于可复现的科学方法本身，特此如实说明。'
)

# ================= 二、科学问题理解 =================
doc.add_heading('二、科学问题理解', level=2)
doc.add_heading('2.1 科学问题与研究对象', level=2)
para(
    '酿酒酵母是模式真核生物，其蛋白质组对化学扰动与遗传背景变化的响应直接关系'
    '"AI 预测 → 湿实验验证"闭环（如菌株定向改造、药物响应预筛）。官方评测将泛化'
    '能力拆为三类：S1 已见菌株 + 未见化合物（新扰动外推）、S2 未见菌株 + 已见化合物'
    '（新遗传背景外推）、S3 双重未知组合，另有时间外推。'
)
para(
    '从数据出发，任务有三个关键难点：（1）小样本高维——训练样本数千、输出蛋白'
    '数千，直接回归极易过拟合到"恢复群体平均状态"而丢失个体响应；（2）实体外推'
    '——未见菌株/化合物没有可学的 ID 嵌入，必须依赖可迁移的特征（结构、序列、遗传'
    '距离）或上下文统计先验；（3）测量上下文混杂——数据来源、仪器、板号等测量变量'
    '与生物条件混叠，需要模型吸收测量变异而不被其误导。'
)

doc.add_heading('2.2 科学意义', level=2)
para(
    '模型的科学意义体现在三处：一是为未见化合物的蛋白质组响应提供可外推的预测'
    '（验证集 FC 预测与真实差异表达呈正相关，预测 Δ 显著富集到已知生物学通路）；'
    '二是菌株遗传距离特征的引入，使"遗传近的菌株响应可迁移"这一生物学直觉进入'
    '模型（test 新菌株 CRD 与训练菌株 CGD 的 SNP 距离仅 0.383，是外推的关键信号）；'
    '三是审计与消融中沉淀的负面结论（如"门控不是瓶颈""排序损失无效"）本身就是'
    '对该数据尺度下方法边界的有价值记录。'
)

# ================= 三、技术方案与预期方法路线 =================
doc.add_heading('三、技术方案与预期方法路线', level=1)

doc.add_heading('3.1 技术方案', level=2)
para('总体形式：将预测拆为"细胞状态"与"扰动响应"两部分——')
para('ŷ(s,c,k) = ŷ_ctrl(s,k) + Δ̂(s,c,k)', size=12, bold=True)
para(
    '其中 ŷ_ctrl 为匹配对照预测（状态模型），Δ̂ 为扰动响应（扰动模型），k 为培养基/'
    '温度/时间/平台上下文。这一拆分与官方评测高度对齐：六模块中"匹配对照原始 FC '
    '（25%）""上下文均值残差（20%）""药物均值残差（20%）"均基于 Δ = y_treat − y_control。'
)
bullet('状态模型（control）：菌株/培养基/温度/时间/平台/上下文先验编码 → MLP → 匹配对照丰度。')
bullet(
    '扰动模型（response）：菌株 + 化合物（Morgan 指纹 + RDKit 10 项描述符）+ 上下文编码 → '
    '低维状态 z（64 维）→ 低秩输出 Δ̂ = U z + 残差 + GO 通路效应 + ESM2 序列先验。'
    '低秩基 U 由训练集 Δ 的 SVD 学习（方差解释约 48%），降低 4422 维输出头的过拟合。'
)
bullet(
    '上下文先验 ctx_prior：菌株×培养基×温度×时间分组均值作为蛋白空间先验（train-only），'
    '为未见菌株提供"细胞状态锚点"，是经验贝叶斯式的保守外推。'
)
bullet(
    '菌株遗传特征：由 1011 酵母菌株 SNP 距离矩阵构建"到训练菌株的遗传距离向量"，'
    '使未见菌株可借遗传近邻迁移响应。'
)
bullet(
    '结构化先验：GO 通路注意力（92 个高频通路，同一通路蛋白共享响应偏置）与 ESM2 序列'
    '先验作为功能层面的软约束。'
)
para(
    '损失函数采用分层组合（分层损失）：绝对丰度 MSE + Δ 监督 MSE + '
    'FC 相关损失（对齐 Δ 预测与 Δ 真值）+ 蛋白谱相关损失 + GO 通路图正则 + 留一组件残差'
    '监督（Δ 对齐去掉自身后 μ_ctx/μ_drug 残差）+ 残差头 L2 正则；按三阶段训练：阶段 A '
    '对照模型预训练 → 阶段 B 响应模型训练（冻结对照）→ 阶段 C 联合微调（分组学习率）。'
)
para(
    '门控设计（v5.3 实验）经历过一次重要迭代：初版为 seen/unseen 硬门控，后按'
    '"相似度/支持数/不确定性"改为连续可靠性门控，验证结果表明门控形式并非当前瓶颈'
    '（与 v4.9 结论一致），故保留简单形式并在文档中如实记录。'
)

doc.add_heading('3.2 预期方法路线', level=2)
para('当前为初赛阶段，以下为已计划/已启动的后续路线（部分已有初步实验）：')
bullet(
    'leave-entity-out episodic training：在训练中随机留出化合物/菌株实体，把其样本按'
    '"未见"处理并加权其扰动损失，使训练目标直接对应 S1/S2/S3 外推。已实现并启动实验'
    '（初赛提交前初步结果未见提升，具体见 4.2，属于值得继续调参的方向而非已定论）。'
)
bullet(
    '自适应化学迁移融合：对未见化合物，按"同菌株条件对齐 + 结构相似度 top-k"加权训练'
    '化合物响应做迁移，融合系数 α 由化合物相似度自适应（在 val 伪新化合物上验证了'
    'α 形式）。后续将把融合系数改为可学习校准器，并加入"响应谱相似性迁移"分支。'
)
bullet(
    '菌株功能聚合特征：SNP 距离核已验证有效；下一步将把拷贝数变异/功能注释映射到通路'
    '层面形成功能聚合特征（当前数据仅提供距离矩阵，功能聚合受数据限制）。'
)
bullet(
    '蛋白模块化输出与不确定性估计：将 4422 维输出改为"通路模块状态 → 蛋白残差"两层'
    '结构；引入 MC-dropout / 深度集成给出预测置信区间，使模型输出可用于虚拟筛选的'
    '"哪些预测可信"判断。'
)

doc.add_heading('3.3 数据来源、依赖工具与运行流程', level=2)
para(
    '数据来源：官方 train_val 与 test（2026-07-15 发布版，官网为准），仅 train 划分用于'
    '训练与全部统计量估计；外部公开资源包括 UniProt GO 注释、ESM2 蛋白序列嵌入、'
    'STRING 蛋白互作、1011 酵母菌株 SNP 距离矩阵、PubChem 化合物 SMILES。'
)
para(
    '依赖工具：Python 3.9 / PyTorch（CUDA）/ scikit-learn（SVD）/ RDKit（描述符与指纹）/ '
    'pandas / numpy；训练在单卡 GPU 上进行。运行流程：数据预处理 → 特征构建 → 三阶段'
    '训练（v2.1/v3.5/v3.7/v5.2）→ 场景评估（5 个随机种子）→ 提交文件生成。'
)

# ================= 四、阶段性实验结果 =================
doc.add_heading('四、阶段性实验结果或可行性验证', level=1)

doc.add_heading('4.1 阶段性实验：方法演进的动机与验证', level=2)
para(
    '以下按时间顺序记录主要方法变更、动机与验证结果，体现"做了什么、为什么、'
    '结果如何"的完整过程。'
)
table(
    ['阶段', '关键变更', '动机', '结果'],
    [
        ['v2.x', 'B/S/C/T 四分支残差分解 + 对照监督 + LOO 组件监督 + 可见性门控',
         '让架构直接对应评测目标（绝对保真/化合物特异/菌株调制）',
         'val 四场景蛋白 R² 中位数约 0.66~0.88，3×v2.1 集成最优'],
        ['v3.x', 'Morgan 指纹、ESM2 先验、STRING PPI、GO 通路注意力',
         '开放榜后引入结构/序列/功能先验，替代纯 ID 记忆',
         'v3.7（GO 通路注意力）为最强单模型，unseen 菌株 +0.013'],
        ['v5.0', '泄漏审计 + control/delta 低秩分离重构',
         '评审路线：审计统计量越界，验证分离架构',
         '修复 3 处泄漏（chem_emb_init 用 val 真值、对照跨划分、迁移池含 val）；'
         '分离单模型不如 v37，但作为集成成分有互补'],
        ['v5.1', '组件级残差监督（μ_ctx/μ_drug LOO）+ RDKit 10 项描述符',
         'v37 靠组件监督强、v5.0 缺它弱；补结构描述符',
         '单模型 strain +0.089 / both +0.094'],
        ['v5.2', '菌株 SNP 遗传距离向量',
         '让未见菌株借遗传近邻迁移（CRD-CGD 距离 0.383）',
         '单模型 strain +0.038 / both +0.031'],
        ['v5.3', '相似度/支持数驱动的可靠性门控',
         '评审建议的软门控替代硬门控',
         '负面：无提升（与 v4.9 一致），门控非瓶颈'],
    ]
)
para(
    '负向与边界结果同样记录：排序方向损失无效（v4.3/v4.7）、Dropout 降低效果、'
    '大模型过拟合（v3.8）、原始 SNP/CNV 直接接入无效、exp(τ) 软最大迁移权重不如'
    'max 加权（0.6337 vs 0.6349）、低秩维度 d=32 与 d=64 持平、episodic training '
    '初版未见提升。这些结果共同界定了该数据尺度下的方法边界。'
)

doc.add_heading('4.2 当前结果', level=2)
para(
    '（1）5 个随机种子的稳定性（val 四场景，蛋白 R² 中位数 mean±std）。'
    '统一方法层面报告 0.75×v3.7 + 0.25×v5.2 集成的表现：'
)
table(
    ['场景', 'v5.2 单模型 R²', 'v5.2 FC PCC', '集成 R²', '集成 FC PCC'],
    [
        ['val_chem_only（S1 新化合物）', '0.829±0.004', '0.338±0.007', '0.869±0.000', '0.426±0.001'],
        ['val_strain_only（S2 新菌株）', '0.581±0.040', '0.280±0.014', '0.690±0.002', '0.355±0.004'],
        ['val_both（S3 双重未知）', '0.669±0.034', '0.223±0.010', '0.762±0.001', '0.234±0.003'],
        ['val_time（时间外推）', '0.779±0.002', '0.512±0.005', '0.832±0.000', '0.611±0.001'],
    ]
)
para(
    '集成结果跨 5 个种子标准差 ≤0.004，说明成绩稳定；v5.2 单模型在 unseen 菌株场景'
    '存在 0.03 量级的种子波动，属于小样本实体的固有噪声，如实报告。'
)
para(
    '（2）测试集真值自评（官方允许"可自评不可训练"，统计量/监督/迁移池全部 train-only）。'
    '按官方六模块评分（绝对保真 20% / 匹配对照 FC 25% / 上下文均值残差 20% / '
    '药物均值残差 20% / 双重未知与时间 10% / 高效应蛋白 DEP 检出 5%），'
    '提交文件 prediction_v52ens.csv 的加权自评：'
)
table(
    ['场景', 'M1 FC', 'M2 绝对', 'M3 ctx', 'M4 drug', 'M5 双盲/时间', 'M6 DEP', '总分'],
    [
        ['test_chem_only', '0.468', '0.922', '0.397', '0.468', '—', '0.624', '0.506'],
        ['test_strain_only', '0.827', '0.769', '0.827', '0.826', '—', '0.682', '0.725'],
        ['test_both', '0.556', '0.808', '0.556', '0.556', '0.605', '0.636', '0.615'],
        ['test_time', '0.624', '0.917', '0.561', '0.562', '0.779', '0.702', '0.677'],
    ]
)
para(
    '加权总分 ≈ 0.626（四场景权重 1/1/0.5/0.5，近似官方组合）。其中 M6 DEP 采用'
    '"方向准确率 40% + 高效应 PCC 30% + F1@K（50/100/200 三档均值）30%"口径，'
    '以固定 top-K 避免"全报阳性"刷高召回（官方规则明确要求）。'
)
para(
    '（3）科学层面的验证：预测 Δ 显著富集到 4 个已知生物学通路（GO 富集分析，'
    'FDR<0.05），说明模型学到的不只是数值拟合，还捕捉到功能层面的响应模式；'
    '菌株遗传距离与响应相似度的正相关进一步支持"遗传近 → 响应可迁移"的生物学'
    '假设（详见复现仓库中的评估脚本与交接文档）。'
)

# ================= 五、复现与开放计划 =================
doc.add_heading('五、复现与开放计划', level=1)

doc.add_heading('5.1 复现方式', level=2)
para(
    '代码仓库（GitHub 公开）按数据/特征/训练/评估/提交组织，训练与评估全部固定随机种子'
    '（v2.1: 42/43/44；v5.2: 1/2/42/43/44），所有统计量（缺失率、均值、标准化、SVD、'
    '迁移池、对照匹配）仅从 train 划分计算；README 提供从数据预处理到提交文件生成的'
    '完整命令序列。复现环境：Python 3.9 + PyTorch + RDKit + scikit-learn，单卡 GPU。'
)

doc.add_heading('5.2 开源计划', level=2)
para(
    '仓库将于复赛阶段公开（初赛按规则不强制提交代码，先说明开源计划与边界）：'
    '开源全部训练/评估/提交脚本与模型权重；外部资源（GO/ESM2/STRING/SNP 距离/'
    'PubChem SMILES）全部披露来源、版本与获取方式；数据本身遵循官方数据使用协议，'
    '不随仓库分发。'
)

doc.add_heading('5.3 依赖、数据来源与合规披露', level=2)
bullet('官方数据：train_val + test（四个文件，蛋白维度 5243），仅 train 划分用于训练与统计量估计；测试真值仅用于最终自评（官方允许"可自评不可训练"）。')
bullet('外部资源：UniProt GO 注释（uniprot_yeast_full.tsv）、ESM2 蛋白嵌入、STRING 蛋白互作（v11.5）、1011 酵母菌株 SNP 距离矩阵（Bergström et al. 2014 数据）、PubChem 化合物 SMILES（11 个未见化合物，用于结构迁移）。')
bullet('依赖：PyTorch（BSD）、scikit-learn、RDKit（BSD 3-Clause）、pandas/numpy（BSD）、python-docx（MIT）；无商业 API、无闭源模型。')
bullet('合规自检：对照匹配、分组均值、SVD、迁移池全部 train-only；未见化合物指纹由 SMILES 生成（不依赖测试真值）；评审可复现性由固定种子 + 完整命令序列保证。')

doc.save(r'D:\leiyuanze\Datawhale\AI for Research\虚拟细胞\初赛方案文档.docx')
print('Saved: 初赛方案文档.docx')
