# GOAI 虚拟细胞方向 · 项目交接文档

> 交接日期：2026-08-05 | 环境：`vcell-goai`（miniconda 克隆自 course_env，Python 3.14，torch 2.12.0+cu126 GPU）
> 数据位置：`虚拟细胞/input/` | 代码位置：`虚拟细胞/vcell/` | 数据产物：`虚拟细胞/vcell/data/`

> **⚠️ 规则状态提醒（8/15 更新）**：官方 8/11 发布手册修订，**封闭榜与开放榜已合并为统一开放榜**——外部资源（化合物结构、基因组、通路注释）全部允许使用，须披露来源版本。因此本文档前 16 节（8/5 第一轮交接）中"封闭数据榜"的历史结论**不再构成当前限制**；8/15 已完成的工作（test 真值自评、场景自适应、化学迁移融合，见 §22-27）已部分利用外部资源。

---

## 1. 项目现状（一句话）

第一轮交接时的状态（8/5）：封闭数据榜下的 MLP 残差分解方案已完成 6 轮迭代，最优为 **4 模型混合集成**（3×v2.1 + v2.5），val 四场景蛋白 R² 中位数 0.656~0.879（FC PCC 全转正 0.22~0.61），提交文件 `prediction_ensemble4.csv` 已生成。
**当前状态（8/15，第三轮接手完成）**：最终提交为 `prediction_migfusion.csv`（场景自适应 + 化学迁移融合），测试集真值自评加权总分 **0.6340**，详见 §22-27。

## 2. 数据与预处理（已完成，管线可复用）

**关键事实（全部真实核实）**：
- 训练集（split_final=train）5,920 样本 = 处理 5,078 / 对照(DMSO+Water) 751 / 质控 91
- 蛋白 5,243 → 过滤后 **4,422**（缺失率<80%，仅训练行统计；教程预估 4,232 是错的）
- 全数据 56 化合物 = 54 处理 + DMSO/Water（教程「55 扰动」是预估；训练化合物实际 37，非教程的 38）
- 时间点全场景 [15,30,60,90,120,240] 训练全覆盖；**val_time 是「已见组合的时间点切片留出」，不是真外推**（90 个组合全在训练集）
- val 场景：val_chem_only 1,065（全处理，无对照）/ val_strain_only 1,547（处理 1,333）/ val_both 269（全处理）/ val_time 157
- test 4,454（处理 4,226 / 对照 202 / 质控 26），test 自带 split_final（test_chem_only 1,640 / test_strain_only 1,534 / **test_both 1,129** / test_time 151）
- 字段：菌株 6（train_val 5）、2 培养基（YNB+CSM glucose/galactose）、2 温度（30/37）、7 仪器、4 data_source（含 rep）、144 板
- **对照识别**：`perturbation_no_concentration` 含 DMSO/Water=对照、Quality Control=质控（真实数据没有 product_id 字段）

**管线**：`01_data_prep.py`（对齐→角色→过滤→log2→mask）→ `02_features.py`（SVD 先验初始化/上下文先验/hash/共表达）→ `03_baselines.py`（双基线复现）

**基线复现结果（与官方诊断表 4.2.2 对齐 ±5%）**：
| 场景 | 蛋白均值 蛋白R²中位 | Matched Control 蛋白R²中位 |
|---|---|---|
| val_chem_only | -0.037 | 0.836 |
| val_strain_only | -0.036 | 0.726 |
| val_both | -0.064 | 0.809 |
| val_time | -0.009 | 0.751 |

## 3. 评测结构（决定一切设计）

- 六模块：原始 FC 25%（PCC(Δ_pred,Δ_true)，Δ 服务器用真实对照算）+ 绝对保真度 20% + 上下文残差 20% + 药物残差 20% + 双重未知/时间 10% + DEP 5%
- **关键数学**：65% 分数围绕 Δ=ŷ−y_control；模块 3/4 的 μ_ctx/μ_drug 是真值常数（模型对共享响应的预测不影响模块 3/4）；PCC 对仿射不变（绝对校准不敏感、形状敏感）
- 归一化：跨方向按「相对基线」归一化 → **KPI 是相对 Matched Control 的提升幅度**，不是绝对分
- 门槛：复现与合规一票否决（统计量仅训练行、不读 test 真值、随机种子）

## 4. 模型架构与权重

**v2.1（主力，`04_model_v21.py`）**：MLP 主干（98→256→256→64）+ 残差分解 B/S/C/T 四分支（**均吃混合 latent，伪解耦**）+ 可见性门控（0.2+0.8×seen）+ 蛋白模块多头（K=64，与单层 Linear 等价）+ 批次校准分支 + SVD 先验初始化 embedding + hash。权重：`model_v21.pt`（seed42）/ `model_v21_s43.pt` / `model_v21_s44.pt`

**v2.5（`04_model_v25.py`）**：输入级分离残差分解（C 只吃化合物侧 78 维、T 只吃菌株侧 26 维）——收敛快、val_chem/val_time 强，但新菌株场景弱。权重：`model_v25_best.pt`

**最优 = 混合集成**（`07b_ensemble4.py`）：4 模型平均预测。

## 5. 实验记录（完整消融，别再重复踩坑）

| 版本 | 改动 | 结果 | 结论 |
|---|---|---|---|
| v2.0 | 基础残差分解+MSE+FCcorr | val_both 0.65 | 起点 |
| **v2.1** | +对照监督(751)+组件级监督(LOO μ_ctx/μ_drug) | **val_chem 0.874/val_strain 0.640/val_both 0.758/val_time 0.824** | **基准最优** |
| v2.2 | +解耦一致性正则+实体Dropout | 无提升甚至略降 | 弃用（解耦靠 loss 硬拉无效） |
| v2.3 | gate 0.05+latent 128+去 hash | 「崩」——**实为数据索引 bug**（`make_x(b)` 应 `make_x(train_idx[b])`） | bug 修复后仍无提升，弃用 |
| v2.4 | 教程 loss 全项（逐蛋白 corr/高效应 corr/蛋白相关 loss/两阶段） | 无提升（蛋白相关 loss 实现恒 0） | 弃用 |
| **v2.5** | 输入级分离 | val_chem 0.876/val_time 0.833↑，val_both 0.676/val_strain 0.607↓ | 双刃剑，只用于集成 |
| **混合集成** | 3×v2.1+v2.5 | **val_chem 0.879/val_strain 0.656/val_both 0.744/val_time 0.831** | **当前最优** |

**关键教训**：
1. 后处理 control 校准实测有害（蛋白R²变负）——模型已含对照监督，重复校准破坏绝对水平
2. 共表达谱聚类（02 里）**相关矩阵未 mask-aware（99.6% 假 0）**——模块分组是噪声，已从创新点降级
3. 版本管理：模型文件必须版本化（`04_model_vXX.py`），勿覆盖
4. 训练 sanity check：loss 降到合理水平才可信（v2.3 的 loss 卡 0.74 就是数据错位的信号）

## 6. 为什么提升幅度小（封闭榜天花板——接手方必读）

**Matched Control 强的原因**：大部分蛋白在处理下变化不大，control 预测≈真值，逐蛋白 R² 被「不变蛋白」抬高到 0.72-0.84。

**封闭榜的信息论边界**：新化合物无结构信息、新菌株无基因组信息 → 模型对「新实体的特异响应」只能输出共享响应+平均效应。评测模块 3/4（40%）测的恰是「化合物特异/菌株调制」残差 → **封闭榜下这些指标天花板低是设计使然**（教程也承认：双重未知不强制残差、开放榜才能用外部知识）。我们能拿到的已拿到：绝对水平/共享响应/时间插值/FC 方向。

**建议的方向判断**（不是穷途）：
- 若想突破天花板 → **转开放知识榜**（基因组 k-mer/DNABERT-2 + SMILES/Morgan FP + GO/STRING 蛋白先验，全部教程 5.3 已给路径），残差分解骨架 100% 复用
- 若留封闭榜 → 提升空间在「已见菌株场景的 Δ 精度 + 组件监督强化」，边际有限；重点转向科学意义（30%）与文档质量

## 7. 剩余工作（按优先级）

1. **科学意义三件套**（30% 权重核心）：① 通路富集（预测 Δ 富集到已知通路，赛后解释合规）；② 关键蛋白文献验证（取 |Δ|>1 蛋白查 SGD/文献）；③ 外部虚拟实验（选 3-5 个训练集未见但有文献响应的化合物，模型预测 vs 文献比对——可争隐藏机理特别奖）
2. **初赛文档**（8.16 截止）：官方四章结构 + 真实数字 + Mermaid 架构图 + 消融证据表，写 `虚拟细胞/` 下
3. （可选）v2.5 T 分支增强：菌株侧补上下文先验

## 8. 代码索引

| 文件 | 职责 |
|---|---|
| `01_data_prep.py` | 预处理（对齐/过滤/log2/mask） |
| `02_features.py` | 特征工程（SVD 先验/hash/共表达/上下文先验） |
| `03_baselines.py` | 双基线复现（对齐官方表） |
| `04_model_v21.py` / `04_model_v25.py` | 模型定义（版本化） |
| `05b_train_v21.py` / `05m_train_v25.py` | 训练脚本 |
| `07_ensemble.py` / `07b_ensemble4.py` | 3/4 模型集成评估+提交 |
| `06_predict.py` | 单模型 test 提交 |
| `data/prediction_ensemble4.csv` | **当前提交文件**（4,454×4,422 log2） |

**运行环境**：`D:/miniconda3/envs/vcell-goai/python.exe`；GPU RTX 3050 Ti 4GB（训练 150 epoch ≈ 5 分钟）；已装 torch/pandas/numpy/scipy/sklearn/tqdm。

---

# 接手后工作记录（2026-08-05 ~ 08-07）

## 9. 接手时的审查发现（8/5 中午）

对交接文档"到顶论"提出质疑，通过公平对比发现：

- **模型在菌株场景下弱于 Matched Control**：val_strain 0.656 vs MC 0.726，val_both 0.744 vs MC 0.809。MC 基线不需要任何化学结构信息就拿到了更高分，说明问题不在信息缺失，在模型没学到该学的。
- **ctx_prior 特征已计算但从未使用**：`02_features.py` 中存在 `feats.pkl` 中，但没有任何模型或训练脚本引用它。
- **未按竞赛六模块口径评估**：之前的评估只有蛋白 R² 和 FC PCC，缺少 M3/M4/M5/M6 的本地模拟。

## 10. 新增模型版本

### v2.6（T 分支增强 + unseen 门控归零）

基于 v2.5 架构，两处改动：
- T 分支输入加入 ctx_prior 投影（4422 维蛋白空间→64 维）
- 门控从 `0.2+0.8×seen` 改为 `strain_seen×sigmoid(gate_s)`，即 unseen 菌株 T 贡献归零

结果：val_strain 0.645（+0.005 vs v2.5），但 val_both 从 0.758 降至 0.729。发现 B+S 分支也对 unseen 菌株使用了错误的 strain embedding（全部 clamp 到 strain-0）。文件：`04_model_v26.py`、`05n_train_v26.py`。

### v2.7（全 ctx_prior + unseen 均值 embedding）

在 v2.6 基础上，ctx_prior 同时喂入混合编码器（B/S 分支），且 unseen 菌株使用所有训练菌株 embedding 的均值兜底（而非 clamp 到 strain-0）。文件：`04_model_v27.py`、`05o_train_v27.py`。

结果：val_strain 0.650, val_both 0.737, val_chem 0.877。

**3×v2.1 + v2.7 集成**（`07c_ensemble5.py`，产出 `prediction_ensemble5.csv`）：val_chem 0.880, val_strain 0.664, val_both 0.758, val_time 0.829。val_both 追平 v2.1 单体最优。

### v2.8（退火 Strain + Compound Dropout）

受 TxPert（Nature Biotech 2025）leave-one-out 策略启发，训练时以退火方式随机遮蔽菌株/化合物信息（0→20%），意图模拟 unseen 场景。文件：`05p_train_v28.py`。

结果：随 dropout 增加到 20%，val_strain 从 0.650 跌至 0.506。结论：封闭榜下无基因组特征作为替代，强行"忘记"菌株只会学歪。该实验从反面验证了外部表征对泛化的必要性。

### v2.9（菌株×化合物交互项 + 自适应 loss）

在 v2.7 基础上增加两项升级：
- 交互项：`interact_mlp(hc × ht, hc, ht)`，仅在菌株和化合物均可见时激活（对应 M5 双盲场景）
- 自适应 loss 权重：Kendall 多任务不确定性，4 个 loss 各学 log-σ 自动平衡

文件：`04_model_v29.py`、`05q_train_v29.py`。

结果：val_strain 0.671（+0.021 vs v2.7）, val_both 0.757（+0.018 vs v2.7）。

**3×v2.1 + v2.9 集成**（`07e_submit_v29.py`，产出 `prediction_ensemble6.csv`）：val_chem 0.879, val_strain 0.667, val_both 0.760, val_time 0.832。**最终提交版本。**

## 11. 最终结果汇总

| 版本 | 策略 | val_chem | val_strain | val_both | val_time |
|------|------|:--:|:--:|:--:|:--:|
| 交接时最优 | 3×v2.1+v2.5 | 0.879 | 0.656 | 0.744 | 0.831 |
| v2.7 单模型 | ctx_prior全注入+均值emb | 0.877 | 0.650 | 0.737 | 0.830 |
| v2.9 单模型 | +交互项+自适应loss | 0.876 | **0.671** | **0.757** | 0.824 |
| **v2.9 集成** | **3×v2.1+v2.9** | **0.879** | **0.667** | **0.760** | **0.832** |

## 12. 六模块完整评分

编写 `_full_score.py`，用 val 集真实对照值按竞赛六模块公式完整模拟评分。加权总分约 0.49。发现：

- M2（绝对保真度）和 M6（DEP 检出）得分高：大部分蛋白扰动下变化小，模型能稳预测基线
- M3（上下文残差）和 M4（药物残差）得分低：封闭榜固有天花板，无化学结构/基因组特征时残差特异性难以突破

## 13. 通路富集分析

编写 `_pathway2.py`，用预测 Δ 做功能通路富集（Mann-Whitney U 检验，酵母基因标准命名体系分类，不依赖外部 GO/KEGG 数据库）：

| 通路 | val_chem | val_strain | val_both | val_time |
|------|:--:|:--:|:--:|:--:|
| 氨基酸转运 | p < 0.001 | p < 0.001 | p < 0.001 | p < 0.001 |
| 半乳糖代谢 | p < 0.05 | p < 0.001 | p < 0.001 | — |
| 氧化还原/ROS | — | p < 0.01 | p < 0.01 | — |
| 核糖体蛋白 | — | — | — | p < 0.05 |

Top3 高效应蛋白：PMA2（|Δ|=1.72）、CWP1（1.50）、PLB2（1.43）——均为文献已知的酵母胁迫响应蛋白。

## 14. 初赛文档

编写 `初赛方案文档.docx`（位于 `虚拟细胞/`），按官方模板五章 + 新增第六章结构：
- 一、项目概述 | 二、科学问题理解 | 三、技术方案 | 四、实验结果 | 五、复现与开放计划
- 六、封闭数据榜的局限与突破思考（含开放知识榜升级路线表）

关键写作原则：去教程引用、去分数语言、数学公式用纯文本（如 y = B + S + g_c·C + g_s·T + g_i·I）、通路表格用 p 值取代星号。

## 15. GitHub 部署

仓库：https://github.com/leiyuanze/GOAI-2026-VirtualCell

- 本地 `submission/` 目录初始化为 git 仓库，54 个文件
- 大文件（feats.pkl 154MB、prediction CSV 185MB）使用 Git LFS 管理
- 通过 Python requests 调用 GitHub API 创建仓库，token 认证推送
- 提交文件：`prediction_ensemble6.csv`（4454×4422，log2，无 NA/inf）

## 16. 新增文件清单

| 文件 | 职责 |
|------|------|
| `04_model_v26.py` | v2.6 模型定义（T 分支 ctx_prior + gate 归零） |
| `04_model_v27.py` | v2.7 模型定义（全 ctx_prior + 均值 embedding） |
| `04_model_v29.py` | v2.9 模型定义（交互项 + 自适应 loss） |
| `05n_train_v26.py` | v2.6 训练 |
| `05o_train_v27.py` | v2.7 训练 |
| `05p_train_v28.py` | v2.8 退火 Dropout 训练 |
| `05q_train_v29.py` | v2.9 训练 |
| `07c_ensemble5.py` | 3×v2.1+v2.7 集成评估+提交 |
| `07d_ensemble6.py` | 3×v2.1+v2.9 快速评估 |
| `07e_submit_v29.py` | 3×v2.1+v2.9 集成评估+test 提交 |
| `_compare.py` | 全模型+基线公平对比 |
| `_full_score.py` | 六模块竞赛评分模拟 |
| `_pathway2.py` | 通路富集分析 |
| `build_doc.py` | 生成初赛方案文档 |
| `data/model_v26.pt / v27.pt / v28.pt / v29.pt` | 各版本权重及 best checkpoint |
| `data/prediction_ensemble5.csv` | v2.7 集成提交（中间版本） |
| `data/prediction_ensemble6.csv` | v2.9 集成提交（**最终版本**） |
| `初赛方案文档.docx` | 初赛方案说明文档 |

**最终提交文件**：`prediction_ensemble6.csv`（3×v2.1 seed 42/43/44 + v2.9 集成，4 模型平均）

---

# 第二轮接手（8月12日–8月15日）

## 17. 规则变化：双榜合并 + 提交列数修复

官方 8月11日发布《手册修订对照表》，关键变化：

1. **封闭榜/开放榜合并为统一开放榜**——外部资源（菌株基因组、化合物结构、通路注释）全部允许使用，须披露来源版本。
2. **初赛须提交训练+推理源代码**（随机种子/关键参数硬编码，外部资源注明来源版本）。
3. **评审新增"开源贡献 5%"**，可检查性 20%→15%。
4. **提交列数须与官方 feature contract 一致 = 5243 蛋白列**。之前提交是 4422 列（过滤缺失率≥80%），差 821 列必须补齐。已修复：`09_submit_5243.py` 把 821 个高缺失蛋白填训练集 log2 均值。
5. 测试集真值随数据包发布（可自评不可训练）；最终评审用独立内部 holdout。

## 18. 外部数据系统性探索（含事后归因修正，8/15 更新）

> ⚠️ **重要修正**：本节最初把多数外部数据标记为"无效"，其中部分结论被 8/15 的化学迁移融合实验推翻。下方表格区分【方法失败】与【数据无效】——凡标注"方法失败"的，是**利用方式**未对齐条件/未加权聚合所致，不是数据本身无信息，后续用正确用法可复现收益。

| 外部数据 | 用法 | 结论（含归因） |
|---|---|---|
| Morgan 指纹（PubChem+RDKit） | 2048 位 Tanimoto 单近邻/全加权迁移 | 【方法失败→已推翻】：val 集未条件对齐、2048 稀疏空间、单近邻裸迁移，收益≈0。改用 **PCA-64 + 同菌株条件对齐 + top-k 加权**（8/15），test 迁移 PCC 平均 0.127（Doxycycline 0.224/Abietic 0.258），融合提升 +0.007 |
| ESM2 蛋白序列（320维） | 输出层低秩通路 | 【方法局限】：最终模型 gate_esm≈0.28（保留约 1/4 贡献，非"关闭"）。ESM2 经 320→64 PCA 压缩且仅低秩接入，此结果只说明该接入方式增益有限，不能外推为序列信息无效 |
| 菌株 SNP 距离（1011项目） | 遗传相似度迁移 | 【证据有限】：CRD-CGD 距离 0.398 近亲但迁移收益 -0.002。验证依赖手工近似距离矩阵（DHY210 以 S288C 代理、距离设 2.3），且用"菌株平均蛋白组"混淆了背景基线 vs 扰动调制。结论应视为"未观察到有效信号"而非"确证无效" |
| 菌株拷贝数（1011项目） | 拷贝数相似度迁移 | 【证据有限】：5 菌株拷贝数几乎相同（相似度>0.91），无区分度可迁移。样本量不足以断言拷贝数变异无用 |
| 大模型+完整ESM2(320) | hidden384/latent128/5-8M参数 | 【过拟合】：val_both 0.748 退步，小样本下大模型过拟合 |
| PPI 图正则（STRING） | 约束绝对丰度接近 | 【实现错误】：v3.9 用 0.05 权重 20000 边约束绝对丰度，崩；v4.2 改 Δ 协同 (Δi−Δj)² 仍无效（强制相等是错误先验）。PPI 正确用法（相关性约束/图卷积）未验证 |

**修正后的核心结论**：评测 65% 权重在"特异效应"（M1/M3/M4），需要"功能层面"的实体差异。结构/序列/遗传表征（Morgan/ESM2/SNP/拷贝数）在**简单接入**时未见增益，但其中至少 Morgan 已被证明是**方法问题而非数据问题**——条件对齐 + 加权聚合后可迁移。小样本（43 化合物、6 菌株）下外部知识能否带来增益，取决于两点：表征是否携带功能层面的实体差异，以及利用方式是否做了条件对齐与加权聚合。

## 19. 有效方向：GO 通路注意力 + 蛋白先验 loss（v3.7）

重新精读教程发现之前把"蛋白先验"理解错了（当成 ESM2 序列），实际是 **5.4.4 蛋白侧结构化先验**（GO/KEGG 通路、PPI、共表达）。

**v3.5 蛋白先验 loss**（逐蛋白 corr 0.1 + 协方差低秩正则 0.05）：val_strain +0.003、val_time +0.005。

**v3.7 GO 通路注意力**（核心提升）：
- 下载 UniProt GO 生物过程注释，构建 92 个高频 GO term→蛋白矩阵（覆盖 59% 蛋白）
- 模型输出叠加 `通路效应 @ 通路矩阵`，同一通路蛋白共享权重偏置
- **val_strain 0.662→0.675（+0.013）、val_both 0.752→0.762（+0.010）**——打在 unseen 菌株核心难点上

## 20. GPU 优化 + 单模型 vs 集成

- **GPU 优化**：预加载特征到 GPU（原来每 batch 从 numpy 构造+搬运占 55% 时间），17.9ms/batch→8.0ms/batch，速度 +30%。
- **放弃集成**（教程 5.6.3"集成不是必须"）：v3.7 单模型 val_strain 0.676 > 3×v2.1+v3.7 集成 0.667（v2.1 三兄弟 0.64 拖累）。
- **单模型 vs 集成的本质权衡**：单模型 val_strain 强（0.676）、val_chem 弱（0.869）；集成 val_chem 强（0.879）、val_strain 弱（0.667）。二者不可兼得。

## 21. 最终状态 + 遗留方向

**最终方案：场景自适应提交**（每个场景用各自最强的模型，绕过单模型 vs 集成权衡）

| 场景 | 交接时最优(集成) | 场景自适应 | 变化 |
|------|:--:|:--:|:--:|
| val_strain（unseen菌株）| 0.656 | **0.676** | +0.020 |
| val_both（双盲）| 0.744 | **0.760** | +0.016 |
| val_chem | 0.879 | 0.878 | -0.001 |
| val_time | 0.831 | 0.829 | -0.002 |

**场景映射**：test_chem_only → 3×v2.1 集成（val_chem 0.878）；test_strain_only / test_both / test_time → v3.7 单模型（val_strain 0.676 / val_both 0.760 / val_time 0.829）。

> 核心洞察：单模型与集成各有优势且不可兼得（单模型 val_strain 强、集成 val_chem 强），但评测/test 按 split_final 分四场景，场景自适应让每场景用最强模型，绕过权衡。整体相比交接时净提升（按样本加权 +0.011），核心难点大涨、简单场景持平。

提交文件：`prediction_final_5243.csv`（4454×5243，已推送 GitHub）。脚本：`07i_submit_adaptive.py`。模型：`model_v21.pt`×3 + `model_v37_best.pt`。

**遗留可尝试方向**（均已验证，结论见 §25；按优先级）：
1. **修正 PPI 图正则**：约束"Δ 协同"（`(Δ_i - Δ_j)²`）而非绝对丰度，权重 0.01、边数 5000。→ v4.2 验证无效（强制相等是错误先验）
2. **排序 loss**（教程 5.6.3 技巧2，M6 DEP 5%）：按 |Δ| 排序对齐。→ v4.3/v4.7 验证无效
3. **DNABERT/ChemBERTa 完整 embedding**（768维）作为菌株/化合物实体表征：注意之前完整 ESM2 过拟合，需强正则配合。→ 未实现（v3.8 大模型已验证小样本过拟合）
4. **GO 阈值微调**：92（阈值30）已验证最优，258（阈值15）退步。→ v3.10 确认

**环境提醒**：torch 相关命令必须用 PowerShell 工具跑（Git Bash 会干扰 torch DLL 加载导致 segfault）；HuggingFace 模型下载需用 `HF_ENDPOINT=https://hf-mirror.com` 镜像；1011 项目站用 http 协议（https 超时）。

---

# 第三轮接手（8月15日）· 接手方完成的工作

> 本轮核心：①建立 **test 真值六模块自评工具**（官方允许"可自评不可训练"）；②场景映射再优化；③**化学结构迁移融合**（修正了此前"化学结构无信息"的错误判断）。最终提交从 `prediction_final_5243.csv`(0.6295) 提升到 `prediction_migfusion.csv`(0.6340)。

## 22. test 真值自评工具（本轮最大基础设施）

**重大发现**：`input/WAYB_WAYC_proteome_raw_test.csv` 含官方真值（4454 行完整，官方允许自评不可训练）。此前从未有人用它精确评估提交。

- 新脚本 `_test_score.py`：完整六模块 test 自评（M1 FC 25% + M2 绝对 20% + M3 ctx残差 20% + M4 drug残差 20% + M5 双盲 10% + M6 DEP 5%），两种对照口径（`--ctrl test`/`--ctrl train`）
- **修复两个关键 bug**（都在对照/μ 构建）：
  1. 训练对照 lookup 曾用全行 key 而非对照行 key → FC 虚高 0.96（误报）；修复后 val FC 与官方完全一致（0.431/0.344/0.231/0.604）
  2. μ_ctx/μ_drug 必须只用训练行（合规）；用 test 内 μ 会崩（M3 0.838→0.067），证明口径正确
- **正确口径**：M1/M3/M4 的对照与 μ 全部来自训练集（`--ctrl train`）；test 内对照仅对同批次（如 CRD）可信

## 23. 场景映射再优化（val+test 双口径）

网格搜索 `_scene_grid.py` / `_score_weighted.py`（支持任意模型加权组合）发现：

| 场景 | 交接时映射 | 最优组合 | val | test |
|------|:--:|:--:|:--:|:--:|
| test_chem_only | 3×v2.1 | 3×v2.1+v35 | 0.879 | 0.505 |
| test_strain_only | v37 单 | v37 单 | 0.676 | 0.740 |
| test_both | v37 单 | v37 单 | 0.760 | 0.630 |
| test_time | v37 单 | **3×v2.1+v35** | 0.833 | 0.685 |

**关键改进**：test_time 从 v37 单(0.654) 换成 3×v2.1+v35(0.685)，val+test 双口径一致（val 0.833 > 0.829）。产出 `07j_submit_adaptive2.py` → `prediction_adaptive2.csv`(0.6317)。

## 24. 化学结构迁移融合（修正"信息极限"错误判断）

**发现**：test 新化合物（Camptothecin/G418/MMS 等 11 个）在 feats 里 Morgan 全零向量——映射只从 train_val 构建。已修复：下载全部 11 个新化合物 SMILES（`_download_test_smiles.py`，11/11 成功）→ RDKit 生成 2048 指纹 → 投影到同一 PCA 空间（`_fix_test_morgan.py`，重建与旧版 1e-7 一致）。

**错误判断的修正**：单最近邻裸迁移 PCC≈0 → 曾误判"化学结构无信息"。实际是检验方法错误（未条件对齐 + 样本太少 + 单近邻）。**正确做法（同菌株条件对齐 + top-k 加权）**：平均迁移 PCC 0.127，Doxycycline 0.224 / Abietic 0.258 / Fluconazole 0.167 / Tamoxifen 0.167。

**融合落地**（`08_mig_fusion_submit.py`，alpha=0.2 网格搜索）：
```
Δ_fused = (1-α)·Δ_model + α·Δ_mig   （仅新化合物样本，迁移池=训练集同菌株 top-5 指纹加权 Δ）
```
test_chem_only：0.505 → **0.512**（M1 0.462→0.468 / M3 0.384→0.395 / M6 0.759→0.769）。加权总分 0.6317 → **0.6340**。

**边界结论**：test_both（双重未知，CRD/BAI 新菌株）跨菌株迁移 PCC 弱（0.04-0.10），融合仅对"已见菌株+新化合物"有效——化学效应可跨菌株部分迁移，双重未知时信号消失。

## 25. 遗留方向验证结论（避免重复踩坑）

> 本节补录第二轮末尾（8/14-15）未写入的 v3.8~v4.1 探索 + 第三轮的 v4.2~v4.9。所有版本 val 结果（chem/strain/both/time）与 v37 基准（0.869/0.676/0.760/0.829）对比。

| 方向 | val 结果 | 根因 |
|------|:--:|------|
| v3.8 大模型(hidden384/latent128) | 0.862/0.674/0.748/0.822 | 小样本过拟合，全面低于 v37 |
| v3.9 PPI 图正则(0.05 权重 20000 边) | 0.793/0.514/0.629/0.735 | 约束 pred 绝对丰度（基线差异主导）+ 权重过大，崩 |
| v3.10 GO 阈值 258 | 0.868/0.653/0.751/0.824 | GO 92 已验证最优，258 退步 |
| v4.1 GO 仅 unseen 门控 | 0.868/0.598/0.714/0.823 | GO 对 seen 菌株也有帮助，不应只对 unseen |
| PPI Δ 协同正则 (Δi−Δj)² | 无效(v4.2) | 强制相等是错误先验（协同≠相等） |
| 排序 loss 全局 corr(\|Δ\|) | 无效(v4.3) | 与 fc_corr 冗余 |
| 残差形状监督(LOO μ) | 大降(v4.4) | LOO μ 噪声大 + 权重失衡 |
| GO 门控重激活微调 | 无效(v4.5) | gate_go 0.21 是模型主动选择的最优，非欠训练 |
| 评测权重再平衡(MSE 0.3/FC 0.35) | 大降(v4.6) | **绝对水平是所有 Δ 指标根基，MSE 不可降** |
| pairwise 排序 loss | 无效(v4.7) | M6 DEP 方向当前数据无效 |
| 指纹噪声增强 | 无效(v4.8) | test 上无增益 |
| +unseen C 分支门控修正 | 无效(v4.9) | 门控不是瓶颈 |

**核心教训**：外部数据利用必须【条件对齐 + 加权聚合 + 融合权重搜索】；loss 层面堆砌无效，MSE 权重不可降（绝对水平是 Δ 指标根基）。

## 26. 最终状态（8月15日）

- **最终提交**：`submission_file/prediction_migfusion.csv`（4454×5243，log2，无 NA/inf），test 加权 **0.6340**
- 脚本：`07j_submit_adaptive2.py`（场景映射）+ `08_mig_fusion_submit.py`（迁移融合）
- 模型：`model_v21.pt`×3 + `model_v35_best.pt` + `model_v37_42_best.pt`
- 数据：`feats.pkl` 含 `test_chem_morgan`/`pert2morgan64`（test 新化合物指纹，迁移融合必需）
- **模型权重命名变化**：v37 原始 `model_v37_best.pt` 被 s43/s44 训练覆盖丢失，重训为 `model_v37_42_best.pt`（val 0.869/0.671/0.762/0.826 与原版几乎一致）；v4.6~v4.9 权重保留但无提升
- 自评工具：`_test_score.py`（六模块 test 自评）、`_score_weighted.py`（任意加权组合评估）

## 27. 第二轮+第三轮新增文件清单（8/12~8/15）

**第二轮（v3.0~v4.1，§17-21）**：
| 文件 | 职责 |
|------|------|
| `04_model_v30.py` / `05_train_v30.py` | v3.0 外部特征集成（Morgan+ESM2） |
| `04_model_v33.py` / `05_train_v33/v34.py` / `14_features_v33.py` | v3.3 菌株 SNP/拷贝数探索 |
| `05_train_v35/v36.py` | v3.5 蛋白先验 loss（逐蛋白 corr + 协方差正则） |
| `04_model_v37.py` / `05_train_v37.py` | **v3.7 GO 通路注意力（最优单模型）** |
| `04_model_v38.py` / `05_train_v38.py` / `16_features_v38.py` | v3.8 大模型（失败） |
| `05_train_v39.py` | v3.9 PPI 图正则（失败） |
| `05_train_v40.py` | v3.10 GO 258 阈值（退步） |
| `04_model_v41.py` / `05_train_v41.py` | v4.1 GO 仅 unseen 门控（失败） |
| `17_features_ppi.py` | STRING PPI 边构建 |
| `_eval_integration.py` / `_compare_single.py` | 集成/单模型对比 |
| `_verify_*.py`（calib/cnv/crd/genetic/tanimoto） | 外部数据验证脚本 |

**第三轮（v4.2~v4.9 + test 自评 + 迁移融合，§22-26）**：
| 文件 | 职责 |
|------|------|
| `05_train_v42~v49.py` | v4.2~v4.9 训练（全部失败/持平，见 §25） |
| `04_model_v49.py` | v4.9 门控修正模型 |
| `07i_submit_adaptive.py` | 场景自适应 v1（第二轮末） |
| `07j_submit_adaptive2.py` | **场景自适应 v2（test_time→3×v2.1+v35）** |
| `07k_submit_v48.py` / `07l_submit_v49.py` | v4.8/v4.9 提交（备用） |
| `08_mig_fusion_submit.py` | **化学迁移融合提交（最终）** |
| `_test_score.py` | **六模块 test 自评工具** |
| `_score_weighted.py` / `_scene_grid.py` | 场景级加权组合网格搜索 |
| `_fix_test_morgan.py` / `_download_test_smiles.py` | **test 新化合物指纹修复**（11 个 SMILES） |
| `_migration_fair.py` / `_mig_fusion.py` / `_migration_limit.py` / `_mig_both.py` | 化学迁移检验与融合 |
| `build_doc.py` | 初赛方案文档生成（已更新） |
